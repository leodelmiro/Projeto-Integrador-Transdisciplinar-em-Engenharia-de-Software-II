spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true
      path: /h2-console

feign:
  okhttp:
    enabled: true